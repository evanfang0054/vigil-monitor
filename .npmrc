# 启用 workspace 功能
# 将所有依赖提升到根目录的 node_modules，以避免重复安装
shamefully-hoist=true

# 禁用严格的 peer dependencies 检查，允许版本不完全匹配
strict-peer-dependencies=false

# 自动安装 peer dependencies，无需手动安装
auto-install-peers=true

# 包管理器限制
# 启用 pre/post 脚本，允许在安装前后执行自定义脚本
enable-pre-post-scripts=true